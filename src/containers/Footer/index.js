import React from "react";
import { Player } from "./Player";
import * as Styled from "./styled";

export const Footer = () => {
  return (
    <Styled.Footer>
      <Player />
    </Styled.Footer>
  );
};
